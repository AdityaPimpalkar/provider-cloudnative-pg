# Provider identity and component definitions.
# Edit this file to add or remove components.
# Run `make generate` after making changes.

components:
    engine:
        type: postgresql
        parametersSchema: CNPGCustomSpec
name: provider-cloudnative-pg
