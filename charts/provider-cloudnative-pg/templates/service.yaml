apiVersion: v1
kind: Service
metadata:
  name: {{ include "provider-cloudnative-pg.fullname" . }}
  labels:
    {{- include "provider-cloudnative-pg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "provider-cloudnative-pg.selectorLabels" . | nindent 4 }}
