{{- if (index .Values "plugin-barman-cloud" "enabled") }}
# Spec is generated by `provider-sdk generate` from definition/ files.
# Run `make generate` to update.
{{ .Files.Get "generated/backupclasses/cnpg-barman-plugin.yaml" }}
{{- end }}
