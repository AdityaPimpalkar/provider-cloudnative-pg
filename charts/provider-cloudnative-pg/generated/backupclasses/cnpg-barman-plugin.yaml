# This file is generated by `provider-sdk generate` from definition/ files.
# Do not edit manually. Run `make generate` to update.
apiVersion: backup.openeverest.io/v1alpha1
kind: BackupClass
metadata:
  name: cnpg-barman-plugin
spec:
  config:
    openAPIV3Schema:
      properties:
        target:
          type: string
      type: object
  description: CloudNativePG backups via the CNPG-I Barman Cloud Plugin (object-store WAL archiving and base backups).
  displayName: CNPG Barman Cloud Plugin
  executionMode: ProviderManaged
  providerManaged:
    limits: {}
  supportedProviders:
    - provider-cloudnative-pg
  uiSchema:
    backup:
      components:
        target:
          fieldParams:
            defaultValue: prefer-standby
            label: Backup target
            options:
              - label: Prefer standby
                value: prefer-standby
              - label: Primary only
                value: primary
          path: target
          uiType: select
      componentsOrder:
        - target
      label: Backup Configuration
